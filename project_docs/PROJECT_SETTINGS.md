# Project Settings

Read `/AGENTS.md` first, then `project_docs/PROJECT_INDEX.md`. Identify the active phase, task category, affected REQ/AC/VAL/RISK/ROUTE IDs and canonical document owners before editing.

Treat the repository and tool output as evidence. Never claim inspection, creation, execution, compilation, testing, runtime behaviour, packaging, deployment or verification unless visible evidence proves the exact scope. Distinguish Observation, Assumption, Proposed, Ran, Passed, Failed, Verified and Unproven.

Inspect directly relevant source, tests, build files, persistence, UI routes, renderer paths and historical reports before changing them. Load the minimum context needed. Preserve existing contracts unless a managed change is explicitly required. Make narrow, reversible, modular and testable changes; avoid unrelated cleanup.

The application is a native offline Windows C++20 fractal-authoring app with file-backed desktop presentation. Keep `src/Core` platform-neutral. Preserve compensated camera coordinates, precision fallbacks, production CPU/D3D11/OpenGL preview/export paths, bounded JSON/data-only equations, local-only behaviour, reversible WorkerW/Progman attachment and the configured Windows wallpaper. Desktop modes are only None, saved Static image, saved-image Slideshow and already-exported local Video; never route desktop presentation through fractal rendering, animation evaluation or renderer fallback.

Do not add network access, telemetry, accounts, cloud services, agents, retrieval, vector storage, queues, workers beyond a demonstrated local-job need, package dependencies, downloaded shaders, executable expressions, shell command templates or bundled FFmpeg without an accepted requirement and decision.

Safeguard startup settings, user files, output paths, logs, imports, migrations, destructive actions, GPU resources, monitor state, sleep/resume, Explorer restart, long-running jobs and cancellation. Use temporary files and validated promotion for settings and output. Preserve original data on migration or parse failure. Bound dimensions, iterations, palette size, journey rows, candidates, thumbnails, memory, workers and retries.

Keep preview/project, user settings and runtime resources conceptually separate. Current models remain authoritative until a domain is migrated through inspected adapters. Do not create a second mutable state authority. Use stable IDs for reorderable structures. Runtime playback, export frames, discovery progress and adaptive-performance events never enter undo history.

For rendering changes, use production renderer code paths and run proportionate visual checks. A source-token check does not prove compilation; a portable core build does not prove MSVC, Win32, GPU, WIC or desktop integration. Native Windows-facing completion requires MSVC build evidence and applicable runtime checks. Record backend, device/driver, dimensions, precision, time/seed, fingerprint, metrics and baseline for visual evidence.

Before release, reconcile CMake, manifest, Win32 resource, build script, installer, documentation and package names to one accepted application version. Run source checks, native Release build, CTest, Windows smoke/desktop-mode tests, applicable visual/migration/cancellation checks and package/upgrade validation. Review package and staged files. Do not add a remote or commit unless authorised.

Update only affected canonical owners and their links/status. Preserve historical `FEATURES-*`, `VERIFICATION-*` and `BUILD-FIXES-*` records; never rewrite them to imply a check was rerun. Report inspected, changed, commands run, passed, failed, skipped, visual/migration artifacts, rollback point and unproven items.

Stop when required authority, version decision, migration policy, destructive approval, external-tool/licensing decision, environment, credentials or safety evidence is missing. Prefer a bounded documented blocker over invented behaviour.

For proposed PH-12+ work, inspect every affected camera parse/persist/fingerprint path and both backend precision policies before editing. Preserve exact values before binary conversion, use only the central planner for algorithm/threshold/fallback selection, keep orbit/reference/correction work generation-safe and bounded, and fail unsupported or unresolved output closed. Run the smallest numerical/structural gate first, then native/backend/cumulative checks. Report camera/reference/upload/delta precision, fixture provenance, CPU/RAM/VRAM/cache/worker/disk/cancellation evidence and all unproven limits.
