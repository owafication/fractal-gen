# Decisions and Change History

**Status:** Current decision register; each decision retains its own acceptance state  
**Purpose:** Own cross-cutting choices, open decisions and governance change history  
**Owner:** Architecture  
**Reading trigger:** Any decision affecting multiple domains, migration, compatibility, release or external dependency  
**Update trigger:** Decision proposed, accepted, rejected or superseded

## Decision states

`Proposed`, `Accepted`, `Rejected`, `Superseded`, `Open`.

## Decision register

| ID | State | Decision | Rationale / consequence |
|---|---|---|---|
| DEC-001 | Proposed | Use `project_docs/PROJECT_INDEX.md` as the compact manifest and routing owner. | Prevents duplicate contracts and excessive context loading. |
| DEC-002 | Proposed | Preserve `FEATURES-*`, `VERIFICATION-*` and `BUILD-FIXES-*` as immutable historical evidence. | Avoids converting prior claims into current evidence. |
| DEC-003 | Proposed | Existing current models remain authoritative; introduce immutable snapshots/adapters first. | Prevents a parallel mutable state system. |
| DEC-004 | Proposed | Separate project, user and runtime state conceptually before deeper migration. | Keeps undo/export/playback boundaries explicit. |
| DEC-005 | Proposed | Use stable object IDs, not indices, for reorderable persisted structures. | Makes history and timeline edits robust to reorder. |
| DEC-006 | Proposed | Use hybrid undo operations: scalar changes, structural operations and subtree replacement. | Avoids brittle scalar emulation for broad changes. |
| DEC-007 | Proposed | CPU output is the canonical portable visual path; D3D11 WARP is preferred for reproducible Windows GPU-like fixtures where compatible. | Reduces driver noise while retaining production paths. |
| DEC-008 | Proposed | Implement deterministic frame-sequence export before encoded video. | Separates rendering correctness from encoder integration. |
| DEC-009 | Accepted for PH-09 | Treat FFmpeg as a user-supplied external executable discovered by explicit selection or `PATH`; do not bundle, download or remember its path. Accept compatible builds by successful version probe plus `libx264` encoder and MP4 muxer capability checks rather than a hard-coded version range. | Keeps licensing/distribution responsibility outside the application package, avoids a settings migration and permits capability-compatible builds while failing closed when required features are absent. |
| DEC-010 | Proposed | Integrate existing Fractal Scout; do not recreate its deterministic candidate engine without inspected evidence. | Avoids duplicate functionality and regression risk. |
| DEC-011 | Accepted | Retain 1.13.1 as the canonical application version and align `resources.rc` to it. | Corrects the supplied metadata defect without inventing a new product release. |
| DEC-012 | Accepted | Use the root containing `CMakeLists.txt`, branch `main`, include imported historical evidence in the baseline commit, and add no remote. | Establishes a reversible local baseline without implying an external repository; baseline commit `8f82119`. |
| DEC-013 | Accepted | CMake project version is the human-edited release authority; other release surfaces are validated against it. | Configuration and CTest now fail on drift; the Windows release script also verifies executable metadata. |
| DEC-014 | Open | Decide future timeline persistence location and project-file boundary. | Affects schema and user workflow. |
| DEC-015 | Accepted by user direction | Split precision-intent ownership: projects/presets persist render-affecting intent; user settings persist defaults for new projects and local presentation preferences. Explicit project intent overrides the user default; device capability, chosen execution path and adaptive presentation state are transient. | Preserves reproducibility while retaining user control and prevents hardware-specific state becoming project truth. |
| DEC-016 | Accepted for bounded continuation | Implement the portable PH-02 CPU fixture foundation after the user reported a successful native build, without treating PH-01 manual/runtime or PH-02 GPU gates as passed. | Advances deterministic safety infrastructure while preserving explicit unproven Windows evidence and phase-completion limits. |
| DEC-017 | Accepted by user direction | PH-02 verification remains pending but does not prohibit implementation of PH-03 or later phases. | Preserves unproven PH-02/native evidence while allowing roadmap progress; pending gates remain required for PH-02 and release-completion claims. |
| DEC-018 | Accepted for PH-03 | Use `mw-render-state-v1`: stable field order, length-prefixed strings, explicit enum names, exact finite IEEE-754 bit-pattern hexadecimal with negative zero canonicalised, and SHA-256. | Provides locale/toolchain-independent canonical bytes and a standard digest without an external dependency; any incompatible change requires a new canonical version. |
| DEC-019 | Superseded by DEC-038 | Treat one preview drag as one coalescing sequence; coalesce wheel zoom only while successive monotonic events remain within 250 ms. | Preserved as the original PH-03 rule; the accepted runtime interaction now uses the unified navigation policy in DEC-038. |
| DEC-020 | Accepted for PH-03 | Classify preset load/import and accepted project dialogs as transactional whole-preset replacements; defer their reversible history representation to PH-05. | Prevents incomplete scalar history from losing nested palette, equation, journey or settings-adjacent project state. |
| DEC-021 | Accepted for PH-04 | Use one runtime-only `ProjectHistory` over typed camera/palette deltas; coalesce only matching origin, gesture token and target set; clear history after changed broad replacements. | Delivers bounded undo without a second authority or partial replay across incompatible preset/dialog state. |
| DEC-022 | Accepted for PH-04 | Use configurable hard containment defaults of 256 entries and 4 MiB estimated operation memory; do not persist history. | Provides a deterministic safety bound while leaving future UX tuning and persisted-history design to measured follow-on work. |
| DEC-023 | Accepted for PH-05 | Retain stable scalar deltas/coalescing for complete registered mutations and use bounded exact before/after `Preset` snapshots for structural or incomplete broad transactions. | Preserves the single authority and exact reversibility without forcing palette stops, equations, journeys or dialog replacements into brittle scalar emulation; supersedes DEC-021 only where it required clearing history after broad replacements. |
| DEC-024 | Accepted for PH-06 | Keep timelines runtime-only and evaluator-owned for PH-06; use caller-supplied bounded stable IDs and stable target names, while deferring persistence and ID generation to DEC-014. | Delivers deterministic evaluation without changing schema 9 or creating a second authority; disabled unknown targets can remain inert, but persisted raw compatibility is not yet claimed. |
| DEC-025 | Accepted for PH-07 | Use a modal candidate timeline editor connected to the existing AppWindow render timer and PH-06 preview clock; keep the accepted timeline runtime-only and use the existing Journey replacement path only for explicit lossless reverse conversion. | Avoids a modeless second authority and schema change, gives Cancel a complete restoration boundary, and preserves one playback-clock authority per domain. |
| DEC-026 | Accepted for PH-08 | Ship PNG sequences first through one production CPU tiled worker; use direct rational index timing, pinned quality, verified temporary promotion, fingerprint-bound typed manifest plus prepared receipts, and pause/release an active wallpaper around the modal route. | Separates deterministic frame correctness from GPU/FFmpeg variability, preserves resumability across interruption, prevents mixed/untracked output, and gives worker/UI/resource lifetimes one explicit boundary. |
| DEC-027 | Accepted for PH-09 | Invoke only the selected FFmpeg executable through `CreateProcessW` with a fixed argument vector, bounded stdout/stderr capture and owned-process cancellation; encode H.264/MP4 to an adjacent temporary file, decode-probe one video frame, then atomically promote. Optional frame cleanup may delete only manifest-tracked sequence files after verified success. | Prevents shell interpolation and arbitrary templates, preserves all source frames on failure/cancellation, avoids cross-volume non-atomic promotion and confines deletion to proven application-owned artifacts. |
| DEC-028 | Accepted by delegated user direction | Exact camera centre and positive half-height become canonical project state; current `CameraState` becomes a derived compatibility/render adapter. Exact decimal input is ASCII-only: optional sign, digits with an optional decimal point, and optional `e`/`E` exponent; whitespace, non-finite spellings and digit separators are rejected. Canonical non-zero values use normalized scientific form with lowercase `e`, one leading significant digit, no redundant zeroes or plus sign; zero is `0`. Bound one value to 16 KiB input, 8,192 significant digits and normalized exponent magnitude 1,000,000. | These bounds comfortably exceed practical server-class archival camera text while bounding allocation, parsing, hashing and migration. Canonical text is accepted before any binary conversion; legacy rendering remains a one-way lossy adapter with explicit reporting. |
| DEC-029 | Accepted; package selected and source-integrated | Use the vendored Boost.Multiprecision 1.83.0 standalone source subset with fixed 512-bit `cpp_bin_float`, expression templates disabled, BSL-1.0, no runtime binary and no configure-time package/network dependency. Keep Boost types private behind the project precision boundary. | Resolves the package/licence stop condition with an inspected, hash-locked and reversible source dependency while preserving separate PH-12 migration, numerical and production-route gates. |
| DEC-030 | Accepted by user direction | One platform-neutral deterministic planner owns algorithm, precision and fallback selection. Persisted exact-camera text has no execution-depth cap; every plan declares its required bits, selected backend, and a visible refusal reason when no backend can meet it. Automatic fallback may move only to a demonstrably equal-or-higher precision path—never silently down to a lower-precision image. | Supports effectively unbounded exploration without misrepresenting a fixed backend as infinite. D3D11/OpenGL report capabilities but do not own policy thresholds. |
| DEC-031 | Accepted by user direction | Initial deep profiles are analytic quadratic Mandelbrot and exact power-2 Tricorn. All other formulas may use ordinary rendering only; any requested deep/perturbation execution fails closed until a separately versioned profile and validation exist. | Retains current supported behaviour while preventing plausible but incorrect deep output. |
| DEC-032 | Accepted by user direction | The current four-float orbit upload is encoding `orbit-float4-v1`, capped at its measured validated envelope. It is never evidence that a 512-bit or future dynamic reference was rendered end-to-end; a plan exceeding the envelope selects CPU/direct correction or refuses GPU export. | Keeps GPU speed optional without placing an artificial depth cap on saved cameras. |
| DEC-033 | Accepted by user direction | Reference generation is an immutable, cancellable, generation-fenced service with byte-accounted cache entries. Per-job and process-wide budgets are configurable and an exhausted budget returns an explicit refusal, not an approximate substitute. | Enables bounded server-class execution while retaining a path to arbitrarily deep coordinates. |
| DEC-034 | Accepted by user direction | Invalid perturbation output must be rebased, directly corrected, or rejected. A completed frame may not contain unresolved perturbation pixels; a job fails if its selected correction budget is exhausted. | Makes correctness authoritative over apparent continuity. |
| DEC-035 | Proposed | Add multiple references only after single-reference validity/correction passes, with deterministic enforced bounds. | Sequences complexity after evidence and prevents unbounded reference/correction growth. |
| DEC-036 | Accepted by user direction | The user selects CPU-reference or GPU renderer before deep frame/video export. The immutable job and manifest record the selected backend and classification; a requested GPU path is available only for a validated profile and fails rather than silently falling back to CPU. | Keeps the export choice explicit and reproducible without allowing a renderer choice to bypass validity checks. |
| DEC-037 | Proposed | Release claims use measured formula/profile/backend/depth limits and prohibit literal infinite/unlimited wording. | Aligns user-facing support with evidence. |
| DEC-038 | Accepted by user direction | Preview pan and wheel share `PreviewNavigation`; navigation events within 500 ms continue one undo block, and an inactivity gap starts a new block. Coalescing merges the union of changed camera fields while preserving earliest-before/latest-after values. | Supersedes DEC-019, matches the user-confirmed runtime behaviour and prevents compensated camera field subsets from fragmenting one navigation action. |
| DEC-039 | Accepted by user direction | Remove continuously rendered Live and Journey desktop modes. Retain None, decoded Static image and decoded Slideshow, and add muted looping playback of an already exported local MP4 through Windows Media Foundation. Migrate saved Live/Journey defaults and the legacy live-start flag to None; no failure path may restart fractal desktop rendering. | Eliminates continual desktop fractal processing while retaining authoring/preview/export animation. Schema 11 persists only the selected local video path and supported default mode; static capture may render once to create a normal image file. |
| DEC-040 | Accepted by user direction | Remove Independent monitor mode and per-monitor preset assignments. Retain Mirror and Span. Schema-11 Independent migrates to Mirror; the obsolete assignment map is discarded and is not written by schema 12. | The assignment UI and persisted map did not affect file-backed desktop output. Removing the inert contract avoids a misleading control path while preserving a deterministic display layout. |
| DEC-041 | Accepted technical debt | Retain MFPlay only for the current bounded local exported-MP4 desktop route while recording that MFPlay is a legacy Windows API. Any broader playback work must evaluate and migrate to `IMFMediaEngine` or the current Windows media-player API under its own lifecycle/evidence batch. | Avoids an unscoped media rewrite while making the obsolete dependency explicit. Asynchronous callback and immediate playback failures must still stop visibly and detach safely. |
| DEC-042 | Accepted by user direction | Editing windows use one process-wide AppWindow edit session. Palette, Equation, Settings, Journey, Timeline, Scout, preset, render, export, file and other auxiliary routes disable the main window and any open Quick Controller until the active window closes; competing posted/tray commands are ignored except Stop and Exit recovery actions. | Prevents two candidate snapshots from committing in reverse close order and restoring stale state. Candidate preview remains isolated, and the accepted dialog commits against the one authoritative AppWindow state. |

## Decision record procedure

For a new decision, record:

- ID and date;
- context and evidence;
- considered options, no more than two when architecture is unresolved;
- decision criteria and default;
- affected requirements/phases/files;
- migration and rollback;
- status and approver.

## Governance change history

| Date | Record | Change |
|---|---|---|
| 2026-07-27 | BR-20260727-01 | Generated canonical Markdown foundation from the supplied 1.13.1 archive and roadmap recommendations. Recorded version mismatch, native-Windows evidence gap, roadmap rebase, existing Scout integration direction and phase sequence. |
| 2026-07-27 | BR-20260727-02 | Accepted 1.13.1 as the version authority, corrected the Win32 resource, added configure/CTest drift detection, added built-executable metadata validation, and recorded portable build/test evidence. |
| 2026-07-27 | BR-20260727-03 | Accepted the local Git baseline, added canonical MSVC presets, isolated test app data, a path-isolation test and a report-producing Windows release validation workflow. |
| 2026-07-27 | BR-20260727-04 | Added the portable production CPU visual-fixture harness, four fixtures, exact repeatability, seam strips, diagnostic metrics/artifacts and deliberate-mutation evidence; retained Windows GPU and baseline approval gates. |
| 2026-07-27 | BR-20260727-05 | Recorded PH-02 verification as pending/non-blocking by user direction; added PH-03 stable parameter descriptors, camera/palette adapters, canonical SHA-256 render fingerprints and portable evidence. |
| 2026-07-28 | BR-20260728-01 | Added transactional parameter mutation batches and migrated main-window brightness, contrast, saturation and colour offset through one validated selective-invalidation path; recorded the Windows release result as user-reported evidence only. |
| 2026-07-28 | BR-20260728-02 | Migrated deliberate main-window camera edits through the same compensated transaction path; retained preview pan/zoom runtime copy-backs outside the bounded claim pending PH-04 gesture/coalescing design. |
| 2026-07-28 | BR-20260728-03 | Added discrete built-in palette selection and explicit mutation-origin/future-history-eligibility reporting; broad palette-dialog replacement and preview gesture coalescing remain separate. |
| 2026-07-28 | BR-20260728-04 | Completed PH-03 implementation with bounded preview gesture coalescing, classified transactional whole-preset replacements, candidate-copy project dialogs and expanded portable verification. |
| 2026-07-28 | BR-20260728-05 | Implemented PH-04 bounded runtime camera/palette undo/redo, rotation editing, gesture coalescing, command shortcuts, branch truncation, history bounds and portable validation. |
| 2026-07-28 | BR-20260728-06 | Implemented PH-05 complete project history with atomic structural snapshots, scalar-completeness fallback, broad dialog/preset/import integration, full replay refresh and portable structural/Scout validation. |
| 2026-07-28 | BR-20260728-07 | Implemented the PH-06 runtime-only deterministic general animation model/evaluator, compensated/log/wrapped interpolation, bounded validation, independent clocks and portable evidence. |
| 2026-07-29 | BR-20260729-01 | Implemented the PH-07 modal timeline editor, preview-clock integration, authoritative keyframe authoring and strict loss-aware Journey adapter without changing schema 9. |
| 2026-07-29 | BR-20260729-02 | Implemented PH-08 deterministic PNG frame sequences with immutable jobs, rational timing, production CPU/WIC streaming, verified promotion, manifest/receipt resume, cancellation/progress, wallpaper pause policy and portable VAL-027–VAL-030 evidence. |
| 2026-07-29 | BR-20260729-03 | Added the explicit `<objbase.h>` dependency required by the PH-08 high-resolution render worker COM apartment APIs after the user-reported Visual Studio 18 2026 compile failure; retained native rebuild confirmation as user-side evidence. |
| 2026-07-29 | BR-20260729-04 | Accepted DEC-009/DEC-027 and implemented PH-09 external FFmpeg discovery, capability validation, fixed-vector H.264/MP4 encoding, bounded process logs, owned cancellation, decode verification, atomic promotion and manifest-scoped optional cleanup. Recorded the corrected PH-08 archive as user-reported building/running and retained exact PH-09 native/real-encoder evidence as pending. |
| 2026-07-30 | BR-20260730-01 | Reconciled the stale PH-03/PH-07 structural verifier to the accepted live `PreviewNavigation` and dialog-preview implementation; accepted DEC-038; integrated the combined deep-zoom supporting design as proposed REQ-021–REQ-040, AC-021–AC-040 and PH-12–PH-15 without schema or renderer mutation. |
| 2026-07-30 | BR-20260730-02 | Recorded user direction for split precision-intent ownership, a reviewed additional high-precision implementation, forward-only exact-camera persistence, user-selected validated export renderer, and PH-12 proceeding with scoped PH-10/PH-11 dependencies. No application source, schema or package dependency was changed. |
| 2026-07-30 | BR-20260730-03 | Selected and packaged Boost.Multiprecision 1.83.0 as the DEC-029 high-precision implementation, accepted BSL-1.0 source-only packaging, added an independent fixed-512-bit reference backend, hash-locked the vendored subset and retained PH-12 production-route gates. |
| 2026-09-19 | BR-20260919-01 | Accepted DEC-039 and replaced continuously rendered desktop Live/Journey with file-backed Static/Slideshow and muted looping exported-MP4 playback; added schema-11 original-preserving migration and removed the live desktop renderer API/fallback paths. |
| 2026-09-23 | BR-20260923-01 | Aligned the current mission, product boundary, roadmap, implementation phases, animation/export ownership, traceability and maintenance guidance with DEC-039. Historical implementation evidence remains historical; no source or persisted schema changed. |
| 2026-09-23 | BR-20260923-02 | Accepted DEC-040/DEC-041; removed inert Independent monitor assignments through schema-12 migration, made asynchronous video and slideshow exhaustion fail closed with visible errors, removed obsolete renderer diagnostics, and recorded MFPlay migration debt. |
| 2026-09-23 | BR-20260923-03 | Reconciled current canonical/REQ/PH/risk statuses; verified bounded desktop media and failure handling; deferred video repaint/control until playback readiness; hardened installer process/file ownership and verified isolated lifecycle. PH-11 remains open and DEC-041/RISK-032 remain accepted technical debt. |
| 2026-09-24 | BR-20260924-02 | Added the DEC-042 process-wide exclusive editor-window session, verified bounded Palette/Equation/Settings competing-route exclusion with a native editor-only fixture, and recorded the FileTracker-limited full-build boundary. |

## Compatibility rule

Later batches must check compatibility with DEC-001–DEC-042 and the frozen paths/IDs in this pack. Superseding a decision requires a new ID or an explicit supersession entry; never silently rewrite the rationale.
