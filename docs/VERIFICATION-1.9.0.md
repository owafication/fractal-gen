# Verification — 1.9.0

Portable verification covers the platform-independent equation model, JSON round trips, bounded validation, escape-time calculations, Julia mode, rational maps, transforms, Newton convergence, orbit-trap metrics, distance estimates, coefficient animation, automatic journey behaviour, adaptive pause logic, and invisible-frame suppression.

Windows-only verification still requires compilation and runtime execution on Windows 10/11 for the Win32 equation editor, persistent preview controls, Quick Controller, OpenGL shader compilation, glow post-process pass, WorkerW attachment, and monitor layouts.
